# macOS
.DS_Store
.AppleDouble
Icon[\r]

# 에디터/IDE
.vscode/
.idea/
*.swp

# 로그·임시 파일
*.log
*.tmp
tmp/

# Claude Code 설정 — 웹 산출물이 아니므로 레포에 올리지 않는다
.claude/

# 민감 파일 안전망 — 예약 확정서 PDF나 토큰이 실수로 들어오는 것 방지
*.pdf
*token*
*.pem

# 로컬 작업 파일 — 레포엔 웹 산출물만 (기획서·예약 기록·프로젝트 가이드는 로컬 전용)
docs/
CLAUDE.md
